version: 1
entries:
  - id: win_node_bin_zero_exe_shim
    title: Windows 下 node_modules/.bin 的 0 字节 .exe 占位 shim
    tags: ["windows", "node", "tooling", "bin", "shim"]
    patterns:
      - "node_modules[\\\\/]\\.bin[\\\\/].+\\.exe"
      - "0-byte|0 byte|spawn\\s+.+\\s+UNKNOWN|No valid .* binary found"
    actions:
      - "不要直接执行/校验 `node_modules/.bin/<tool>.exe`（可能是 0 字节占位）。优先 `npm run <script>` / `npx <tool>`，或在 Windows 上改用 `<tool>.cmd`。"
      - "如果脚本里硬编码了 `<tool>.exe`：把它替换为 `<tool>.cmd`（让 cmd shim 去定位真实二进制）。"
      - "如果 `node_modules/.bin/<tool>.exe` 根本不存在但 `<tool>.cmd` 存在：这通常也是 npm 在 Windows 的正常布局；脚本应优先使用 `<tool>.cmd`。"

  - id: node_redeclare_dirname_filename
    title: Node CommonJS 中重复声明 __dirname/__filename 导致 SyntaxError
    tags: ["node", "cjs", "syntaxerror"]
    patterns:
      - "SyntaxError: Identifier '__dirname' has already been declared|SyntaxError: Identifier '__filename' has already been declared"
    actions:
      - "在 CommonJS 下，`__dirname`/`__filename` 已由 Node 提供；删除脚本里 `const __dirname = ...` / `const __filename = ...` 的重复声明即可。"
      - "如果项目是 ESM（package.json: type=module），不要用 `__dirname`；用 `import.meta.url` + `fileURLToPath()` 计算目录并使用不同变量名（不要叫 `__dirname`）。"

  - id: win_npm_bin_shim_cmd_ps1
    title: Windows 下 npm 工具 shim：优先使用 .cmd/.ps1，而不是 .exe/无后缀
    tags: ["windows", "node", "npm", "tooling", "shim"]
    patterns:
      - "node_modules[\\\\/](?:\\.bin|bin)[\\\\/].+(?:\\.exe)?"
      - "No valid .* binary found|binary found but is 0-byte|0-byte|spawn\\s+.+\\s+UNKNOWN"
    actions:
      - "Windows/npm 往往只保证 `node_modules/.bin/<tool>.cmd`/`<tool>.ps1` 可用；`.exe` 可能不存在或是 0 字节占位。"
      - "脚本不要 `spawn('.../.bin/<tool>.exe')` 或用 `stat.size>0` 去“验真”；优先执行 `<tool>.cmd`，或直接用 `npm exec -- <tool>` / `npx <tool>`。"
      - "如果脚本同时有 `node_modules/.bin/` 和 `bin/` 手动回退：只把 `.bin` 那段改成 `.cmd`，`bin/` 回退保持原二进制扩展名。"
      - "如果脚本写成了 `node_modules/bin/<tool>.exe`（缺少点）：改为 `node_modules/.bin/<tool>.cmd`。"

  - id: hugo_bin_vendor_fallback
    title: hugo-bin 在 Windows 下的 vendor 二进制回退（避免 .bin/.exe 入口坑）
    tags: ["windows", "node", "hugo", "hugo-bin", "tooling"]
    patterns:
      - "No valid Hugo binary found|Hugo binary found but is 0-byte"
      - "hugo-bin"
      - "node_modules[\\\\/](?:\\.bin|bin)[\\\\/]hugo\\.exe"
    actions:
      - "优先入口：`node_modules/.bin/hugo.cmd`（由 npm shim 调用 `hugo-bin/bin/cli.js`，再定位真实二进制）。"
      - "如果项目脚本硬编码检查 `node_modules/.bin/hugo.exe`：不要做 size>0 校验；改为优先 `.cmd`，并在 Windows 下增加 vendor 回退：`node_modules/hugo-bin/vendor/hugo.exe`。"
      - "紧急但不推荐：如果你必须满足某个脚本只认 `.bin/hugo.exe`，可把 `node_modules/hugo-bin/vendor/hugo.exe` 复制到 `node_modules/.bin/hugo.exe`（注意：重新 `npm install` 可能覆盖，最好改脚本或加 postinstall）。"

  - id: py_package_shadow_root_module
    title: Python 同名包/根模块骨架冲突（包入口指向不存在的子模块）
    tags: ["python", "imports", "package", "skeleton"]
    patterns:
      - "StaticIssue:\\s*py_package_shadow_root_module"
      - "No module named ['\"][A-Za-z_][A-Za-z0-9_]*\\.[A-Za-z_][A-Za-z0-9_]*['\"]"
      - "from \\.app import app"
    actions:
      - "不要先补依赖；先检查仓库是否同时存在 `foo.py` 和 `foo/__init__.py`。这通常是骨架冲突，不是环境问题。"
      - "如果 `foo/__init__.py` 写了 `from .foo import ...`，但 `foo/foo.py` 并不存在：要么把真实入口移动到 `foo/foo.py`，要么让 `foo/__init__.py` 直接导出正确入口。"
      - "统一导入契约：测试、包入口、实际实现文件三者必须一致；不要让测试 `from foo import app`，包却再去引用不存在的 `foo.foo`。"

  - id: py_missing_local_export_symbol
    title: Python 本地模块导出名与调用方不一致
    tags: ["python", "imports", "exports", "symbols"]
    patterns:
      - "StaticIssue:\\s*py_missing_local_export_symbol"
      - "cannot import name"
      - "has no attribute"
    actions:
      - "先做静态核对：查看调用方 `from module import name` 里的 `name`，再对照目标模块真实导出符号。"
      - "如果目标模块定义的是别名或更长名字（如 `insert_policy_rule`），优先决定是补兼容导出，还是统一修改调用方，不要两边同时猜。"
      - "包根 `__init__.py` 也是导出面的一部分；如果调用方从包根导入，确保 `__all__` 和显式 re-export 一致。"

  - id: py_local_call_signature_mismatch
    title: Python 本地函数调用签名与实现不一致
    tags: ["python", "api-shape", "call-signature"]
    patterns:
      - "StaticIssue:\\s*py_local_call_signature_mismatch"
      - "takes \\d+ positional argument"
      - "got an unexpected keyword argument"
    actions:
      - "优先检查调用点与本地函数定义的参数个数/名称是否一致，不要先归因到环境。"
      - "如果调用方传参而实现不接收，先决定谁是权威接口：是修函数签名，还是修调用点。"
      - "接口一旦定下来，要同步修改 README/示例/测试，避免后续 fix-loop 再把旧调用方式写回来。"

  - id: py_exception_taxonomy_split
    title: Python 异常体系分裂（同名异常在多个本地模块里各自定义）
    tags: ["python", "exceptions", "contract"]
    patterns:
      - "ContractIssue:\\s*py_exception_taxonomy_split"
      - "exception_taxonomy_mismatch"
      - "ValidationError"
    actions:
      - "确定唯一的异常真相源：例如统一从 `error_handler.py` 导出 `ValidationError` / `PolicyFileNotFoundError`，其余模块只 import，不再重复定义。"
      - "不要把内置异常名（如 `FileNotFoundError`）重新绑定到自定义类；用新的业务异常名并在捕获内置异常后再转换。"
      - "测试、API 层、解析层必须共享同一套异常类型，否则 fix-loop 会一直在‘抛错对了但类型不对’上打转。"

  - id: py_engine_interface_drift
    title: Python 引擎接口漂移（调用层和模型/引擎提供的接口不一致）
    tags: ["python", "interfaces", "contract", "engine"]
    patterns:
      - "ContractIssue:\\s*py_engine_interface_drift"
      - "engine_interface_mismatch"
      - "has no attribute"
    actions:
      - "先定单一接口契约：例如 `match()` 返回 bool 还是 reason，`is_valid()` 还是 `is_effective()`，由谁作为权威。"
      - "统一后只改一边：要么补兼容方法/别名，要么统一修改调用方；不要同时猜测多套接口。"
      - "规则引擎、模型、API 层都要跟着同一接口走，并更新测试样例，避免下一轮又把旧接口写回来。"

  - id: py_data_shape_contract_drift
    title: Python 数据形状契约漂移（样本 JSON / 测试 / 模型解析不一致）
    tags: ["python", "data-shape", "json", "contract"]
    patterns:
      - "ContractIssue:\\s*py_data_shape_contract_drift"
      - "data_shape_mismatch"
      - "AttributeError: 'str' object has no attribute 'get'"
    actions:
      - "先决定唯一数据契约：例如 `conditions` 是 `dict` 还是 `list[dict]`，样本、模型、测试只能有一个真相源。"
      - "如果 `from_dict()` 假设字段是列表，就不要让测试数据继续传字典；反之亦然。"
      - "统一后同步修模型解析、示例 JSON、测试断言，不要只修其中一层。"
